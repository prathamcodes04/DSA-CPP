#include<bits/stdc++.h>
using namespace std;
int main(){
    //1D ARRAY
    // int a, b, c, d;
    // cin >> a>> b>> c>>d;
    // long arr[5];
    // long long arr[5];
    //every thing has to be of similar datatype  
    // int arr[5];
    // cin >> arr[0] >> arr[1] >> arr[2] >> arr[3] >> arr[4];
    // arr[3] = 2;
    // cout << arr[3];

    //2D ARRAY
    // int arr[3][5]; // rows and columns
    // arr[1][3] = 78;
    // cout << arr[1][3];
    // return 0;

    //strings
    string s = "pratham";
    int len = s.size();
    s[len-1] = 'z';
    cout << s[len - 1];
    return 0;
}